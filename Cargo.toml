[package]
name = "nblf-queue"
version = "0.1.0"
edition = "2024"

[features]
default = ["tagged-ptr"]
std = ["alloc"]
alloc = []
tagged-ptr = []

[dependencies]
cfg-if = "1.0.3"
portable-atomic = {version = "1.11.1", default-features = false, features = ["require-cas"] } #features = ["fallback"] # (for testing AtomicU128)
